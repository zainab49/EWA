import React from 'react';
import ReactDOM from 'react-dom';
import StationeryForm from './App';

ReactDOM.render(
  <React.StrictMode>
    <StationeryForm />
  </React.StrictMode>,
  document.getElementById('root')
);